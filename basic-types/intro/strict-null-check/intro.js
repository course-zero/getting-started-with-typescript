// variable declarations
var person;
var age = null;
var car = 'Mercedes';
var canDrive = false;
console.log("before => (person:" + person + "), (age:" + age + "), (car:" + car + "), (canDrive:" + canDrive + ")");
// override values
person = 'Ross Geller';
age = 21;
car = undefined;
canDrive = null;
console.log("before => (person:" + person + "), (age:" + age + "), (car:" + car + "), (canDrive:" + canDrive + ")");
